# Phase 2: Implementation (run AFTER Phase 1 review is accepted)
# Run with: ./claude-workers.sh plans/scr-phase2-implement.txt
# Each worker implements one gap from ARCHITECTURE.md
# Primary reviews for conflicts between workers before merging

You are implementing Gap 1 (HIGH): Analysis → Plan Coefficient Handoff. Working in /Users/stevejoy/Developer/SCR. Read ARCHITECTURE.md for context, then read physiology.py (calibrate_banister), training_plan.py (_forecast_vdot_trajectory), and app.py. Your task: (1) Make training_plan.py accept calibrated Banister coefficients (k1, k2, intercept, beta_wbgt) from the analysis/backtest results instead of using literature defaults. (2) Add a function to load the latest calibration from the database or backtest output. (3) Fall back to literature defaults when no calibration exists. (4) Update any app.py routes that generate plans to pass calibrated coefficients. (5) Add tests in tests/test_training_plan.py for both calibrated and fallback paths. Do NOT modify unrelated code.

You are implementing Gap 5 (LOW but quick win): WBGT Heat Correction. Working in /Users/stevejoy/Developer/SCR. Read ARCHITECTURE.md, then physiology.py (wbgt_vdot_correction) and athlete_model.py (build_run_analysis). Your task: (1) Call wbgt_vdot_correction() in build_run_analysis() when WBGT data is available, so per-run VDOT estimates account for heat. (2) Ensure the correction is applied before VDOT is stored. (3) Add a flag or field so the UI can show "heat-adjusted" vs raw VDOT. (4) Add tests in tests/test_physiology.py for the correction integration. Do NOT modify unrelated code.

You are implementing Gap 4 (MEDIUM): Taper & Deload. Working in /Users/stevejoy/Developer/SCR. Read ARCHITECTURE.md and training_plan.py fully. Your task: (1) Implement 50% volume reduction in the final 2 weeks before a race (taper). (2) Implement deload weeks every 3-4 weeks with 60% volume. (3) Implement 10%/week progressive volume increase within phases. (4) Ensure taper maintains intensity but reduces volume (not the other way around). (5) Add tests in tests/test_training_plan.py covering taper, deload, and progression logic. Do NOT modify unrelated code.

You are implementing Gap 3 (MEDIUM): Plan Confidence from Calibration Quality. Working in /Users/stevejoy/Developer/SCR. Read ARCHITECTURE.md, then physiology.py (calibrate_banister return values), training_plan.py, and templates/index.html (Training Plan section). Your task: (1) Surface R², MAE, and bias from calibration results alongside the training plan. (2) Add a confidence indicator: high (R²>0.7), medium (0.4-0.7), low (<0.4 or no calibration). (3) Flag low-confidence plans as "literature-based" in the UI. (4) Add the confidence data to the plan generation API response. (5) Add tests for confidence classification. Do NOT modify unrelated code.
